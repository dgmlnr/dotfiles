# hexdev — discrete AMD Radeon (Ellesmere, RX 470/480/570/580).
#
# No overrides. The shared config is what this machine wants; the GPU has the
# headroom that hexdev-home's HD 4600 does not, so the cursor shader keeps
# animating continuously here.
#
# The file exists because the include in config.tmpl is not optional: ghostty
# fails to load a config-file it cannot find. Keep it, even while it is empty.
